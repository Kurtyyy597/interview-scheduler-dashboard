import type { FilterInterview } from './../../types/interview/filterInterview';

export const initialFilterInterview: FilterInterview = {
  search: "",
  filterStatus: "scheduled",
  filterType: "all"
  
}